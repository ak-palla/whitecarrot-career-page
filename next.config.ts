import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // serverActions config removed - not supported in Next.js 16.1.1
  // Body size limits can be configured via environment variables or API routes
};

export default nextConfig;
